services:
  app:
    image: codex-test-local-app
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - target: 3000
        published: "3000"
        host_ip: 127.0.0.1
        protocol: tcp
        mode: host
    environment:
      NODE_ENV: production
    restart: unless-stopped
